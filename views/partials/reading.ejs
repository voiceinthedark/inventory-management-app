<section class="list">
    <h2>Currently Reading</h2>
    <ul>
        <% readingList.forEach(function(book) { %>
            <li>
                <strong><%= book.title %></strong> by <em><%= book.author_name %></em>
            </li>
        <% }); %>
    </ul>
</section>
